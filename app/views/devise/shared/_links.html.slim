- if devise_mapping.omniauthable?
    - resource_class.omniauth_providers.each do |provider|
      = link_to "Twitterアカウントでサインイン", provider: OmniAuth::Utils.camelize(provider), omniauth_authorize_path(resource_name, provider), method: :post
